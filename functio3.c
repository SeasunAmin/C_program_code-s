#include<stdio.h>
double triangelarea(double base, double height)
int main()
  {
    double base,height;
    printf("Base = ");
    scanf("%lf",&base);
    printf("Hight= ");
    scanf ("%lf",&height);
  double area = triangelarea (base ,height);
  printf("The area is  = %.2lf\n",area);

  }
{
    return 0.5 * base * height;
}
